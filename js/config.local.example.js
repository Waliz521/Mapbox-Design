/**
 * Local override for Mapbox token — copy this file to config.local.js and add your token.
 * config.local.js is gitignored. Vercel uses MAPBOX_ACCESS_TOKEN env var at build time.
 * Get a token at https://account.mapbox.com/
 */
mapboxgl.accessToken = 'pk.YOUR_MAPBOX_ACCESS_TOKEN_HERE';
